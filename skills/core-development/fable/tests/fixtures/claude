#!/usr/bin/env bash

set -euo pipefail

model=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --model)
      model="${2:-}"
      shift 2
      ;;
    *)
      shift
      ;;
  esac
done

[[ -n "$model" ]] || { echo "mock claude: missing --model" >&2; exit 2; }
printf '%s\n' "$model"
