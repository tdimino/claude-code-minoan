# shape

Plan the UX and UI for a feature before writing code. Runs a structured discovery interview, then produces a design brief (`.design-context.md`) that guides implementation. Establishes design direction, constraints, and strategy—design planning only, no code output.
