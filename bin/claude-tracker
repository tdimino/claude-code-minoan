#!/usr/bin/env bash
# claude-tracker — list recent Claude Code sessions with live status
# Thin wrapper: the one implementation lives in the claude-tracker-suite skill.
exec node "$HOME/.claude/skills/claude-tracker-suite/scripts/list-sessions.js" "$@"
