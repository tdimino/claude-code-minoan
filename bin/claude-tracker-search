#!/usr/bin/env bash
# claude-tracker-search — search Claude Code sessions (FTS5 transcript index)
# Thin wrapper: the one implementation lives in the claude-tracker-suite skill.
exec node "$HOME/.claude/skills/claude-tracker-suite/scripts/search-sessions.js" "$@"
